# my-portfolio
my portofolio - explain my profile covered my experience in programming, skils, and project
# Andika Portfolio (Vue 3 + Tailwind + GitHub Pages)

## Run locally
```bash
npm install
npm run dev
```

## Deploy to GitHub Pages
- Push to `main`. Workflow `.github/workflows/pages.yml` will build & deploy.

## is-a.dev JSON
Create PR in `is-a-dev/register` with:
`domains/andikakamiswara.json`
```json
{
  "owner": { "username": "andikatfb0", "email": "EMAIL_KAMU" },
  "records": { "CNAME": "andikatfb0.github.io" }
}
```
